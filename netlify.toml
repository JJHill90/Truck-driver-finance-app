[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev"
  targetPort = 5173

# SPA fallback so client-side routing resolves to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
